---
name: REJECTED
description: >
  Rejected vulnerability workflow.
priority: 3
conditions:
  - condition: OR
    requirements:
      - has label auto-rejected
      - has label rejected
states:
  - name: DONE
    description: >
      A flaw was rejected - there is nothing more to be done.
    jira_state: Closed
    jira_resolution: Won't Do
    requirements: []
